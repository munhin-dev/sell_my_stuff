const cart = require("./cart");
const order = require("./order");
const product = require("./product");
const user = require("./user");
const address = require("./address");

module.exports = { cart, order, product, user, address };
